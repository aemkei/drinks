{
  "name": "diffords",
  "version": "1.4.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm run version-sync && vite",
    "build": "npm run version-sync && vite build",
    "preview": "vite preview",
    "version-sync": "node scripts/version_sync.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.2",
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.1",
    "vite": "^8.0.8"
  }
}
